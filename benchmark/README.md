The `cupbc` subpackages is benchmarked by `benchcupbc.py` and `benchcupbcMg.py`. The CPU version can be installed from `https://code.byted.org/gz.137/CPU_RSDF`.

Other subpackages together with their multi-GPU performance are benchmarked by `benchmark.py`.